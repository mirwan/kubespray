[Service]
Environment={% if http_proxy is defined %}"HTTP_PROXY={{ http_proxy | regex_replace('%', '%%') }}"{% endif %} {% if https_proxy is defined %}"HTTPS_PROXY={{ https_proxy | regex_replace('%', '%%') }}"{% endif %} {% if no_proxy is defined %}"NO_PROXY={{ no_proxy }}"{% endif %}
